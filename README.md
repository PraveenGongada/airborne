# Airborne Test Artifacts

Orphan branch holding locally-built SDK artifacts for CI/local consumer testing.
Built by `scripts/push-test-artifacts.sh` on the airborne fork.

```
maven/   Android SDK (maven layout)
ios/     Airborne.xcframework.zip
rn/      airborne-react-native-*.tgz
```

See `LOCAL_TESTING.md` on the code branch for the build loop.
